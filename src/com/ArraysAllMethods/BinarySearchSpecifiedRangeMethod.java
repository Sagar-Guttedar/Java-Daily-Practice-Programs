package com.ArraysAllMethods;

//Performs a binary search within a specified range of a sorted array using a custom comparator.
// The fromIndex and toIndex parameters define the inclusive range of the search.

import java.util.Arrays;
import java.util.Comparator;

/*public class BinarySearchSpecifiedRangeMethod {
    public static void main(String[] args) {
        // Method 3: binarySearch(array, fromIndex, toIndex, key, Comparator)

        int[] arr = {1, 4, 7, 10, 13};
        int fromIndex = 1;
        int toIndex = 4;
        int keyToSearchInRange = 10;
        Comparator<Integer> comparator = Comparator.naturalOrder();
        int indexInRange = Arrays.binarySearch(arr, fromIndex, toIndex, keyToSearchInRange, comparator);
        System.out.println("Index of " + keyToSearchInRange + " in range [" + fromIndex + ", " + toIndex + "]: " + indexInRange);
    }
}*/
